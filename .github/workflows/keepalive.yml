name: Keep the database awake

# Pings the sync server's /health every ~10 minutes. The host (Vercel) needs no
# keeping awake, but every request reads Supabase Postgres, and a free Supabase
# project PAUSES after ~7 days of inactivity — this is what stops that. Free:
# GitHub Actions is unlimited on public repos. Scheduled runs can be delayed a
# few minutes under load, so the step wakes-and-retries to cover a cold start.
on:
  schedule:
    - cron: '*/10 * * * *'
  workflow_dispatch: {}          # lets you run it manually from the Actions tab

# Don't pile up runs if one is slow waking a cold server.
concurrency:
  group: keepalive
  cancel-in-progress: false

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping /health (wake + retry)
        run: |
          URL="https://garage-saathi-sync.vercel.app/health"
          for i in 1 2 3 4; do
            code=$(curl -s -o /tmp/h.json -w "%{http_code}" --max-time 90 "$URL" || echo 000)
            if [ "$code" = "200" ]; then
              echo "OK ($code): $(cat /tmp/h.json)"
              exit 0
            fi
            echo "attempt $i got HTTP $code — database may be waking, retrying in 20s"
            sleep 20
          done
          echo "health check failed after retries"
          exit 1
